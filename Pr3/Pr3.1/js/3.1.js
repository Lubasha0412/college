let a = prompt("Введите первое число"),
    b = prompt("Введите второе число");

let c = (+a + +b);

let d = (+a - +b);

let e = (+a * +b);

let f = (+a / +b);

alert("Сложение: "+ c + "\n" + "Вычтание: " + d + "\n" + "Умножение: " + e + "\n" + "Деление: " + f);